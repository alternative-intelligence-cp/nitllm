// main.npk — nitllm entry point

use "util/failsafe_handler.npk".*;

use "core/generate.npk".*;
use "../bench/run_throughput.npk".*;

pub func:main = int32() {
    print("Initializing NITLLM Engine...\n");  
    _?generate();  
    _?benchmark();
    print("Inference generation completed successfully.\n");  
    exit(0i32);  
};
