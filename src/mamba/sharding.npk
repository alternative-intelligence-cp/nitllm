// sharding.npk — Tensor Sharding for Mamba Layers
pub func:shard_tensor = int64(int64:size, int64:shards) requires shards > 0i64, size % shards == 0i64 {
    int64:chunk = size / shards;
    pass(chunk);
};
