// parallel.npk — GPU Block-Parallel Execution Logic
pub func:mamba_parallel_exec = NIL(int64:blocks) {
    int64:i = 0i64;
    while (i < blocks) {
        print(`Executing block &{i}\n`);
        i = i + 1i64;
    }
    pass(NIL);
};
