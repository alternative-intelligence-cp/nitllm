// math_extract.npk — Strict unwrapping

use "matmul.npk".*;

pub func:safe_attention = NIL(wild flt32->:q, wild flt32->:k, int64:dim, wild flt32->:out_tile) requires q != NULL, k != NULL, dim > 0i64 {
    _?compute_attention_tile(q, k, dim, out_tile);
    pass(NIL);
};
